{
  "$schema": "../../../../node_modules/@tauri-apps/cli/schema.json",
  "identifier": "default",
  "description": "Capability for the main window — minimal surface for v0.1.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default"
  ]
}
